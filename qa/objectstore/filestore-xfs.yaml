overrides:
  ceph:
    fs: xfs
    conf:
      osd:
        osd objectstore: filestore
        osd sloppy crc: true
        osd op queue: wpq
  ceph-deploy:
    fs: xfs
    filestore: True
    conf:
      osd:
        osd objectstore: filestore
        osd sloppy crc: true

