overrides:
  ceph:
    log-ignorelist:
    - but it is still running
    - overall HEALTH_
    - \(PG_
    - \(MON_DOWN\)
    - \(AUTH_BAD_CAPS\)
tasks:
- workunit:
    clients:
      client.0:
        - mon/caps.sh

